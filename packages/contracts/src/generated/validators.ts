/**
 * AUTO-GENERATED by packages/contracts/src/codegen/generate.ts — DO NOT EDIT.
 * Source of truth: contracts/schemas/*.schema.json. Run `pnpm --filter
 * @redai/contracts run codegen` to regenerate.
 */

import type {
  Approval,
  BrowserNavigate,
  EnrollmentRequest,
  Event,
  ExportManifest,
  FileRead,
  FileWrite,
  Finding,
  HeartbeatRequest,
  HttpRequest,
  LeaseClaims,
  Message,
  Project,
  Reviewer,
  Run,
  RunCreate,
  Scope,
  Snapshot,
  Summary,
  TaskEnvelope,
  TerminalExecute,
  WorkerResult,
} from './types.js';
import { assertValidFor } from '../validate.js';

/** Assert `data` is a valid Run (contract key: run). */
export function validateRun(data: unknown): asserts data is Run {
  assertValidFor('run', data);
}
/** Validate and return `data` typed as Run, or throw. */
export function parseRun(data: unknown): Run {
  validateRun(data);
  return data;
}

/** Assert `data` is a valid Scope (contract key: scope). */
export function validateScope(data: unknown): asserts data is Scope {
  assertValidFor('scope', data);
}
/** Validate and return `data` typed as Scope, or throw. */
export function parseScope(data: unknown): Scope {
  validateScope(data);
  return data;
}

/** Assert `data` is a valid Finding (contract key: finding). */
export function validateFinding(data: unknown): asserts data is Finding {
  assertValidFor('finding', data);
}
/** Validate and return `data` typed as Finding, or throw. */
export function parseFinding(data: unknown): Finding {
  validateFinding(data);
  return data;
}

/** Assert `data` is a valid Reviewer (contract key: reviewer). */
export function validateReviewer(data: unknown): asserts data is Reviewer {
  assertValidFor('reviewer', data);
}
/** Validate and return `data` typed as Reviewer, or throw. */
export function parseReviewer(data: unknown): Reviewer {
  validateReviewer(data);
  return data;
}

/** Assert `data` is a valid Summary (contract key: summary). */
export function validateSummary(data: unknown): asserts data is Summary {
  assertValidFor('summary', data);
}
/** Validate and return `data` typed as Summary, or throw. */
export function parseSummary(data: unknown): Summary {
  validateSummary(data);
  return data;
}

/** Assert `data` is a valid Event (contract key: event). */
export function validateEvent(data: unknown): asserts data is Event {
  assertValidFor('event', data);
}
/** Validate and return `data` typed as Event, or throw. */
export function parseEvent(data: unknown): Event {
  validateEvent(data);
  return data;
}

/** Assert `data` is a valid ExportManifest (contract key: export-manifest). */
export function validateExportManifest(data: unknown): asserts data is ExportManifest {
  assertValidFor('export-manifest', data);
}
/** Validate and return `data` typed as ExportManifest, or throw. */
export function parseExportManifest(data: unknown): ExportManifest {
  validateExportManifest(data);
  return data;
}

/** Assert `data` is a valid HttpRequest (contract key: tool-input.HttpRequest). */
export function validateHttpRequest(data: unknown): asserts data is HttpRequest {
  assertValidFor('tool-input.HttpRequest', data);
}
/** Validate and return `data` typed as HttpRequest, or throw. */
export function parseHttpRequest(data: unknown): HttpRequest {
  validateHttpRequest(data);
  return data;
}

/** Assert `data` is a valid TerminalExecute (contract key: tool-input.TerminalExecute). */
export function validateTerminalExecute(data: unknown): asserts data is TerminalExecute {
  assertValidFor('tool-input.TerminalExecute', data);
}
/** Validate and return `data` typed as TerminalExecute, or throw. */
export function parseTerminalExecute(data: unknown): TerminalExecute {
  validateTerminalExecute(data);
  return data;
}

/** Assert `data` is a valid FileRead (contract key: tool-input.FileRead). */
export function validateFileRead(data: unknown): asserts data is FileRead {
  assertValidFor('tool-input.FileRead', data);
}
/** Validate and return `data` typed as FileRead, or throw. */
export function parseFileRead(data: unknown): FileRead {
  validateFileRead(data);
  return data;
}

/** Assert `data` is a valid FileWrite (contract key: tool-input.FileWrite). */
export function validateFileWrite(data: unknown): asserts data is FileWrite {
  assertValidFor('tool-input.FileWrite', data);
}
/** Validate and return `data` typed as FileWrite, or throw. */
export function parseFileWrite(data: unknown): FileWrite {
  validateFileWrite(data);
  return data;
}

/** Assert `data` is a valid BrowserNavigate (contract key: tool-input.BrowserNavigate). */
export function validateBrowserNavigate(data: unknown): asserts data is BrowserNavigate {
  assertValidFor('tool-input.BrowserNavigate', data);
}
/** Validate and return `data` typed as BrowserNavigate, or throw. */
export function parseBrowserNavigate(data: unknown): BrowserNavigate {
  validateBrowserNavigate(data);
  return data;
}

/** Assert `data` is a valid LeaseClaims (contract key: worker.LeaseClaims). */
export function validateLeaseClaims(data: unknown): asserts data is LeaseClaims {
  assertValidFor('worker.LeaseClaims', data);
}
/** Validate and return `data` typed as LeaseClaims, or throw. */
export function parseLeaseClaims(data: unknown): LeaseClaims {
  validateLeaseClaims(data);
  return data;
}

/** Assert `data` is a valid TaskEnvelope (contract key: worker.TaskEnvelope). */
export function validateTaskEnvelope(data: unknown): asserts data is TaskEnvelope {
  assertValidFor('worker.TaskEnvelope', data);
}
/** Validate and return `data` typed as TaskEnvelope, or throw. */
export function parseTaskEnvelope(data: unknown): TaskEnvelope {
  validateTaskEnvelope(data);
  return data;
}

/** Assert `data` is a valid WorkerResult (contract key: worker.WorkerResult). */
export function validateWorkerResult(data: unknown): asserts data is WorkerResult {
  assertValidFor('worker.WorkerResult', data);
}
/** Validate and return `data` typed as WorkerResult, or throw. */
export function parseWorkerResult(data: unknown): WorkerResult {
  validateWorkerResult(data);
  return data;
}

/** Assert `data` is a valid EnrollmentRequest (contract key: worker.EnrollmentRequest). */
export function validateEnrollmentRequest(data: unknown): asserts data is EnrollmentRequest {
  assertValidFor('worker.EnrollmentRequest', data);
}
/** Validate and return `data` typed as EnrollmentRequest, or throw. */
export function parseEnrollmentRequest(data: unknown): EnrollmentRequest {
  validateEnrollmentRequest(data);
  return data;
}

/** Assert `data` is a valid HeartbeatRequest (contract key: worker.HeartbeatRequest). */
export function validateHeartbeatRequest(data: unknown): asserts data is HeartbeatRequest {
  assertValidFor('worker.HeartbeatRequest', data);
}
/** Validate and return `data` typed as HeartbeatRequest, or throw. */
export function parseHeartbeatRequest(data: unknown): HeartbeatRequest {
  validateHeartbeatRequest(data);
  return data;
}

/** Assert `data` is a valid Project (contract key: api.Project). */
export function validateProject(data: unknown): asserts data is Project {
  assertValidFor('api.Project', data);
}
/** Validate and return `data` typed as Project, or throw. */
export function parseProject(data: unknown): Project {
  validateProject(data);
  return data;
}

/** Assert `data` is a valid RunCreate (contract key: api.RunCreate). */
export function validateRunCreate(data: unknown): asserts data is RunCreate {
  assertValidFor('api.RunCreate', data);
}
/** Validate and return `data` typed as RunCreate, or throw. */
export function parseRunCreate(data: unknown): RunCreate {
  validateRunCreate(data);
  return data;
}

/** Assert `data` is a valid Snapshot (contract key: api.Snapshot). */
export function validateSnapshot(data: unknown): asserts data is Snapshot {
  assertValidFor('api.Snapshot', data);
}
/** Validate and return `data` typed as Snapshot, or throw. */
export function parseSnapshot(data: unknown): Snapshot {
  validateSnapshot(data);
  return data;
}

/** Assert `data` is a valid Message (contract key: api.Message). */
export function validateMessage(data: unknown): asserts data is Message {
  assertValidFor('api.Message', data);
}
/** Validate and return `data` typed as Message, or throw. */
export function parseMessage(data: unknown): Message {
  validateMessage(data);
  return data;
}

/** Assert `data` is a valid Approval (contract key: api.Approval). */
export function validateApproval(data: unknown): asserts data is Approval {
  assertValidFor('api.Approval', data);
}
/** Validate and return `data` typed as Approval, or throw. */
export function parseApproval(data: unknown): Approval {
  validateApproval(data);
  return data;
}
